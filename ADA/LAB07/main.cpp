#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;cin>>n;
    vector<int> arr;
    int a;
    while(cin>>a){
        arr.push_back(a);
    }
    sort(arr.begin(), arr.end());

    


    return 0;
}